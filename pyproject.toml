[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "spark"
version = "0.1.0"
dependencies = [
    "forge"
]
[tool.setuptools.packages.find]
include = ["spark*"]
